# Bắt buộc Docker build — tránh lỗi "Error creating build plan with Railpack"
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# Healthcheck: bật sau khi deploy ổn — /health (xem RAILWAY-DEPLOY.md)
[deploy]
restartPolicyType = "ON_FAILURE"
