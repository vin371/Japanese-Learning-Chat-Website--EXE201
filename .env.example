# Sao chép thành .env ở thư mục gốc repo (docker compose đọc file này).

# PostgreSQL local (Docker) — user yumegoji, DB yumegoji, host port 5433
POSTGRES_PASSWORD=Yumegoji_Pg_2024!

JWT_KEY=change-this-docker-jwt-key-minimum-32-characters-long

# Tuỳ chọn — cổng API trên máy host (container vẫn listen 8080 bên trong)
API_HOST_PORT=5056

# Supabase (dev trên máy — KHÔNG dùng cho docker compose postgres service):
# Tạo backend/appsettings.Secrets.json — xem backend/SUPABASE-CAU-HINH.txt
